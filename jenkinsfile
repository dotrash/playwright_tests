pipeline {
   agent { docker { image 'mcr.microsoft.com/playwright:focal' } }
   stages {
      stage('install dependencies') {
         steps {
            sh 'npm install'
         }
      }
      stage('run tests') {
         steps {
            sh 'npm test'
         }
      }
   }
}